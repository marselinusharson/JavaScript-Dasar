{
  const nilai = "A";

  let tipeData = typeof nilai;
  document.writeln(`<p>${tipeData}</p>`);
}
{
  const nilai = true;

  let tipeData = typeof nilai;
  document.writeln(`<p>${tipeData}</p>`);
}
{
  const nilai = 100;

  let tipeData = typeof nilai;
  document.writeln(`<p>${tipeData}</p>`);
}
{
  const nilai = null;

  let tipeData = typeof nilai;
  document.writeln(`<p>${tipeData}</p>`);
}
